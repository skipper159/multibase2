# Frontend Production Configuration
# NOTE: VITE_API_URL is overwritten at install time by the installer.
# Do not set a real URL here — it will be replaced during deployment.
VITE_PORT=5174
VITE_API_URL=
